# Vendored dependencies

`ws/` is [websockets/ws](https://github.com/websockets/ws) version 8.18.3,
MIT licensed. Runtime files only. See `ws/LICENSE` and the repository
`THIRD_PARTY_NOTICES.md`.

The Relay imports this copy so it does not implement RFC 6455 framing itself.
